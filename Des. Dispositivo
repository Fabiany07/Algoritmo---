import { useState } from "react"; //hulk: capturar o usúario e senha 
import {
    StyleSheet,
    Text,
    TextInput,
    TouchableOpacity,
    View
} from "react-native";
import { useState } from 'react';


export default function Login() {
    const [usuario, setUsuario] = useState();
    const[senha, setSenha] = useState();
    function validaLogin(){
        if((usuario == 'teste') && (senha == '123')){
            console.log('Acertou');

        }
    }
    return (
        <View style={styles.container}>
            <Text style={styles.titulo}>Só Alegria!</Text>

            <TextInput
                style={styles.input}
                placeholder="Usuário"
                onChangText={setUsuario}
                value={usuario}
            />

            <TextInput
                style={styles.input}
                placeholder="Senha"
                onChangeText={(setSenha)}
                value={senha}
                secureTextEntry={true}// adiciona os asteriscos p/ senha 
            />

            <TouchableOpacity style={styles.button} onPress={validaLogin} >
                <Text style={styles.titulo}>Login</Text>
            </TouchableOpacity>
        </View>
    );
}

const styles = StyleSheet.create({
    container: {
        flex: 1,
        backgroundColor: '#000060',
        alignItems: 'center',
        justifyContent: 'center',
        padding: 24,
    },

    titulo: {
        fontSize: 24,
        color: '#fff',
        marginBottom: 20,
    },

    input: {
        width: '90%',
        height: 45,
        backgroundColor: '#fff',
        borderColor: '#000',
        borderWidth: 1,
        fontSize: 14,
        marginBottom: 16,
        color: '#000',
        paddingHorizontal: 10,
    },

    button: {
        backgroundColor: '#fff',
        borderWidth: 1,
        borderColor: '#000',
        width: '40%',
        height: 45,
        justifyContent: 'center',
        alignItems: 'center',
    },

    buttonText: {
        fontSize: 18,
        color: '#000',
        fontWeight: 'bold',
    },
});
